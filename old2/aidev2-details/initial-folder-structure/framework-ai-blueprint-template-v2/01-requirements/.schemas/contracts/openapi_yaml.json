{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "OpenAPI Contract",
  "description": "OpenAPI contracts are plain YAML text files (.yaml) following the OpenAPI specification (e.g. 3.0.x or 3.1.x). They define HTTP API paths, methods, request/response schemas, and status codes. Validation is performed by OpenAPI tooling, not by this schema. This schema exists only to document the convention.",
  "type": "string"
}
