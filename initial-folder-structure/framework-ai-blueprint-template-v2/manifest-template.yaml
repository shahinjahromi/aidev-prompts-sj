# Application requirements manifest template.
# Copy this to ../<<APP_REPO_DIR>>/manifests/requirements-manifest.yaml
# and replace placeholders with your project values.
#
# iteration_id is developer-controlled. Update it manually to include
# requirements from that iteration in the diff/plan/execute pipeline.
# Tooling never auto-updates this field.

manifest_version: '1.0'
requirement_set_id: "<<APP_SLUG>>"
app_identifier: "<<APP_SLUG>>"
implementation_id: "<<IMPLEMENTATION_ID>>"
iteration_id: 1
requirements_version_target: 1.0.0
requirements_version_implemented: 0.0.0
requirement_baseline: []
