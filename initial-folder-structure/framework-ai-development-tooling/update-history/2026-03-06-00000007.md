# Update 2026-03-06-00000007

## Changes

- Added shared, app-agnostic Node.js interface extraction script:
  - `interface-extractors/extract-nodejs-library-interfaces.py`
- Script accepts parameters:
  - `--project-root` (target app root)
  - `--output` (yaml output file)
  - `--source-dirs` (optional, comma-separated; default `server,client,scripts`)
- This enables reuse by spec prompts via relative paths from implementation folders.
