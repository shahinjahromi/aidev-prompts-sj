# 2026-03-06-00000006

## Summary
Removed the scope/context command from tooling. Scope remains in the delta for execution; the standalone command that wrote `context_scope.md` is no longer used.

## Changes
- **Deleted** `calculate_scope.py`: Removed the script that resolved scope to file paths and wrote `.cursor/context_scope.md`.
- **Modified** `ai-tooling.sh`: Removed `scope` action and removed `calculate_scope.py` from the `all` sequence. Updated usage line to list actions without scope.
- **Modified** `README.md`: Removed scope command from examples and from section "4) Generate AI coding inputs"; removed `context_scope.md` from outputs.
- **Modified** `readme_samples.txt`: Replaced "diff + scope" with "diff" only; removed scope command lines and `context_scope.md` reference.
- **Modified** `readme-samples.md`: "Review delta and scope" → "Review delta".
