## Tooling Repo Updates for Requirement Relocation

- Added missing moved items to tooling requirements store:
  - `NFR-021` under `nfr_and_global_cr`
  - `DEC-001` under new `decisions` group
  - File: `tooling-requirements.yaml`
- Updated command docs to include explicit merged-refresh step from diffs:
  - `README.md`
  - `readme-command-sequence.txt`
  - `readme_samples.txt`
- Added alias action `refresh-merged` in `ai-tooling.sh` to call `merge_requirements.py` (same behavior as `merge` / `update-merged`).
## 2026-03-06-00000003

### Changes made
- Added `update-merged` action alias in `ai-tooling.sh` (maps to `merge_requirements.py`).
- Updated `README.md` workflow to include an explicit "update merged requirements from diffs" step.
- Added `update-merged` sample command and corrected `AI_DEV_IMPL_ROOT` in `readme_samples.txt`.

### Why
- Makes the merge refresh action explicit in command sequences.
- Clarifies that merged outputs should be refreshed from diff artifacts before downstream delta/plan generation.

