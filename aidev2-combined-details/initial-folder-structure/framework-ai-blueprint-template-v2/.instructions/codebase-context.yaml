# ROOT: All paths in this file are relative to the blueprint root unless otherwise specified.
# Relative path from this file to blueprint root: ../
#
# Per-implementation tech stack, file maps, ports, npm scripts, and conventions.
# Read by all aidev-* prompts via BLUEPRINT_ROOT/.instructions/codebase-context.yaml.
#
# Fill in the values below for your implementation. Uncomment and complete the
# sections relevant to your tech stack. Keep <PLACEHOLDER> values as reminders
# for fields that must be provided — do not leave them unresolved in active use.

implementations:
  <<IMPLEMENTATION_ID>>:
    project:
      name: "<PROJECT_NAME>"
      monorepo: false
      # package_manager: npm   # set to npm for Node projects; omit for Go/JVM/other
      # workspaces: [client, server]
      # root_package_json: package.json

    tech_stack:
      # server:
      #   runtime: <RUNTIME>
      #   framework: <FRAMEWORK>
      # client:
      #   framework: <UI_FRAMEWORK>
      #   language: <LANGUAGE>
      #   styling: <STYLING_APPROACH>
      # testing:
      #   e2e: <E2E_TOOL>
      #   unit: <UNIT_TEST_TOOL>

    ports:
      # server: <PORT>
      # client: <PORT>

    env_vars:
      # required: [<ENV_VAR>]
      # optional: [<ENV_VAR>]
      # env_file: <PATH>

    npm_scripts: {}
      # root:
      #   dev: "<COMMAND>"

    # All paths below are relative to the implementation's application_root.
    # server_layout:
    #   entry: <PATH>
    #   config: []
    #   routes:
    #     mount_prefix: <PREFIX>
    #     files: []
    #   services: []
    #   middleware: []
    #   utils: []

    # client_layout:
    #   entry: <PATH>
    #   routes: <PATH>
    #   core: []
    #   layout: []
    #   features: []

    database:
      contract_catalog: 01-requirements/03-current/models_and_contracts.yaml
      contract_specs_root: 01-requirements/03-current/models_and_contracts
      pending_contract_specs_root: 01-requirements/01-pending-promotion/models_and_contracts
      physical_schema_mac_id: <<DB_MAC_ID>>
      notes: []

    e2e_tests:
      root: 06-e2e-tests
      config: 06-e2e-tests/playwright.config.ts
      helpers: 06-e2e-tests/helpers/
      api_tests: 06-e2e-tests/api/
      ui_tests: 06-e2e-tests/ui/
      # specs: []

    other:
      manifests_dir: .aidev/requirements/
      requirements_manifest: .aidev/requirements/requirements-state.yaml
      # local_dev_script: scripts/local-dev.sh

    conventions: {}
      # server_routes: "<CONVENTION>"
      # server_services: "<CONVENTION>"
      # client_components: "<CONVENTION>"
